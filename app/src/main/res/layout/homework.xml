<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/cardBottomMargin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/homeworkName"
            android:text="HW Default Name"
            style="@style/HomeworkFloatingText" />

        <TextView
            android:id="@+id/homeworkDueDate"
            android:text="No Due Date Given"
            style="@style/HomeworkFloatingText" />

        <TextView
            android:id="@+id/linkedCourse"
            android:text="No Course Given"
            style="@style/HomeworkFloatingText" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <ProgressBar
                android:id="@+id/progressBar"
                style="@android:style/Widget.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minWidth="@dimen/progressMinWidth"
                android:minHeight="50dip"
                android:progress="0"
                android:progressDrawable="@drawable/custom_progress_bar"/>
            <TextView
                android:id="@+id/percentage"
                android:layout_centerInParent="true"
                style="@style/ProgressBarFloatingText"
                />
        </RelativeLayout>

    </LinearLayout>

</android.support.v7.widget.CardView>
